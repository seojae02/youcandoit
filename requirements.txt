# --- 웹 서버 ---
fastapi
uvicorn[standard]
python-multipart        # UploadFile(파일 업로드) 처리에 필요
pydantic

# --- 영상 분석 ---
opencv-python
mediapipe>=0.10.30,<1.0   # 1.0.x 는 macOS 에서 Metal 초기화 실패로 크래시함
numpy

# --- 음성 분석 ---
openai-whisper
librosa
pyaudio                 # 실시간 마이크 입력에만 필요 (녹화 분석만 쓸 경우 생략 가능)

# --- LLM ---
google-generativeai
python-dotenv
