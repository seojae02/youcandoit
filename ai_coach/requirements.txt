# 배포 이미지에 들어가는 최소 의존성.
# 서버 마이크가 필요한 음성 분석은 requirements-local.txt 로 분리했다.

# --- 웹 서버 ---
fastapi
uvicorn[standard]
python-multipart        # UploadFile(파일 업로드) 처리에 필요
pydantic

# --- 영상 분석 ---
opencv-python-headless  # 컨테이너에는 libGL.so.1 이 없어 GUI 빌드는 import 부터 실패한다
mediapipe>=0.10.30,<1.0 # 1.0.x 는 macOS 에서 Metal 초기화 실패로 크래시함
numpy

# --- LLM ---
google-generativeai
python-dotenv
